<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DailyTaskManager.Views.NewItemPage"
             Title="New Item">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Nombre" FontSize="Medium"/>
            <Entry Text="{Binding Item.Name}" FontSize="Small" x:Name="TxtNombre"/>
            <Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" x:Name="TxtDescription" />
            <Label Text="Fecha" FontSize="Medium" />
            <DatePicker x:Name="FechaPicker" DateSelected="FechaPicker_DateSelected" BindingContext="{Binding Item.Date}" />
            <Label Text="Lugar" FontSize="Medium" />
            <Editor Text="{Binding Item.Place}" FontSize="Small" Margin="0" x:Name="TxtPlace" />
            <Label Text="Prioridad" FontSize="Medium" />
            <Editor Text="{Binding Item.Priority}" FontSize="Small" Margin="0" x:Name="TxtPriority"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
